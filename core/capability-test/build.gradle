plugins {
    alias(libs.plugins.kotlin.jvm)
}

kotlin {
    jvmToolchain(21)
}

dependencies {
    api project(':core:capability-api')
    api libs.kotlinx.coroutines.core
    testImplementation libs.junit
}
